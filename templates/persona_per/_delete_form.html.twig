<form method="post" action="{{ path('app_persona_per_delete', {'idPer': persona_per.idPer}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ persona_per.idPer) }}">
	<button class="btn">Delete</button>
</form>

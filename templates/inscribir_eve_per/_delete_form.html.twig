<form method="post" action="{{ path('app_inscribir_eve_per_delete', {'idEvePer': inscribir_eve_per.idEvePer}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inscribir_eve_per.idEvePer) }}">
    <button class="btn">Delete</button>
</form>
